# 更新日志

本文件记录本项目的重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v0.1.0] - 2026-09-11

首个公开版本：把长期自用的「抖音火花续期」工具整理为可开源形态。

### 特性

- **多账号隔离（MAI-001）**：每个抖音号独立目录 `userdata/accounts/<别名>/`，登录态、目标会话、
  发送文案、执行记录完全互不影响；跨进程 `.running` 守卫保证同一时刻绝不并发。
- **常驻守护定时（SCH-001）**：任务条目库 `userdata/scheduler_jobs.json` + 常驻进程
  `scheduler_daemon.py`——约 20 秒主循环、到点自动切换对应账号串行执行、同刻自动错峰 ≥15 分钟、
  错过当天时刻恢复后补跑一次；心跳写 `scheduler_heartbeat.json`，开机经注册表 Run 自启，
  **不依赖 Windows 任务计划程序**。
- **一键出发批量执行（BAT-001）**：`batch_runner.py` 以独立进程串行跑完全部账号，面板关闭不丢队列。
- **本地管理面板（PAN-001）**：纯标准库 HTTP 服务 + 单页前端，三块（一键触发 / 定时任务 /
  执行记录），支持目标会话勾选、前台触发、定时任务增删改、运行证据截图与审计 JSON 查看。
- **反自动化防护**：反指纹注入 + 仿人操作（曲线移动、按字节奏打字、目标间随机等待），
  只往当前会话聊天框填字、显式排除 IM 搜索框。
- **安全验证优雅停手**：检测到验证码 / 滑块 / 拼图立即终止本次发送并标记「需验证」，
  交由用户在浏览器里手动处理，绝不停留、不破解。
- **审计与证据**：每次运行产生独立目录，关键节点（打开会话、发送后、失败审计）自动截图 +
  结构化 JSON 证据，可区分「真的找不到会话」与「误判」。
- **自检驱动开发**：本项目无单元测试框架，`verify.py` 覆盖定时链路、配置风险、
  会话查找与发送强校验链等 150 项断言，全部通过时退出码为 0。

### 说明

- 运行环境：Windows 10/11 + Python 3.10+ + **本机已安装 Chrome**
  （`config.yaml` 的 `browser.channel: "chrome"`，用于规避 Playwright 自带 Chromium 的风控）。
- `browser.headless` 必须保持 `false`：实测无头模式会被抖音风控 100% 拦截。
- 非官方项目，与抖音 / 字节跳动无任何关联；使用风险自负，详见 README「免责声明与合规风险」。
