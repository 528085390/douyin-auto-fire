# 发送成功判定可信化（编辑器落地正向证据 + verify.py 修绿）— 设计文档（spec）

- 日期：2026-09-04
- 状态：待用户签字
- 决策来源：2026-09-04 全流程体检（读码 + 真实运行日志回放 + verify.py 实测）发现两处 P0；
  用户拍板「自动重试一次再阻断」；用户要求按 superpowers 流程出 spec → plan 再实施

---

## 一、背景与问题

### 1.1 事故链路（2026-09-04 01:22 真实运行日志回放）

最近一次真实运行 `userdata/runs/20260904_012220`（meta：status=success，failed=0，2/2 目标）：

```
01:22:40 [WARNING] 输入后发送按钮未变红，可能内容没进编辑器   ← 目标1 私聊「<会话3>」
01:22:41 [证据截图] 001_012241_sent_<会话3>.png      ← 强校验通过（气泡回读含发送文本）
01:23:22 [WARNING] 输入后发送按钮未变红，可能内容没进编辑器   ← 目标2 群聊「<会话4>」
01:23:23 [WARNING] 气泡回读软校验未通过（输入框已清空，按发送成功处理）：from_me=True 气泡文本='<会话7>'
01:23:23 [证据截图] 002_012323_sent_soft_<会话4>.png     ← 软校验降级放行
01:23:23 [进度] 本次续火花完成 ✅（共 2 个目标）              ← meta 记 success
```

两处关键矛盾：

1. **同一轮两次「按钮未变红」，结果却相反**：私聊目标气泡回读命中发送文本（真发出），
   群聊目标气泡回读只读到旧消息「<会话7>」（新消息气泡缺席）。→ 「发送按钮是否变红」
   不可靠，既不能作成功证据，也不能作失败证据。
2. **群聊目标全程没有正向证据**，却按成功收场。事后无法判定「发出了但气泡读慢/合并」
   还是「根本没发出」（打字被弹层/焦点问题吞掉，编辑器从头到尾为空）。

### 1.2 根因一：清空铁证在空编辑器上「空真」

`douyin.py::_send_text` 现行顺序（:647-696）：

```
点击编辑器 → _human_type(text)
→ publishRedBtn 未变红仅 logger.warning（:652-653，不阻断）
→ 按 Enter
→ 铁证：编辑器已清空（:667-669）→ 未清空才 send_fail
→ 气泡软校验（:684-696）：失败一律降级放行，记 sent_soft
```

问题：**「输入框已清空」这条铁证，只在文字确实进过编辑器时才有意义。**
若打字压根没落地（编辑器全程为空）：

- Enter 是空操作；
- 「清空」判定恒真（空 → 空，真空真）；
- 气泡读不到新文本 → 软失败 → 仍降级放行、仍记 success。

即：决策 1B「必须有正向证据，不能没报错即成功」存在一个未被堵住的空真漏洞，
而代码里唯一能提前发现它的信号（按钮未变红）只打了行 warning。

### 1.3 根因二：回归网与文档已漂移（verify.py 自检 RED）

- `verify.py` 实测 **56 通过 / 1 失败，exit code 1**（README 约定「全部通过时退出码为 0」）。
  失败项：断言审计 tag `"verify_soft_fail"` 字面量存在于 douyin.py —— 但 commit 6b80b9a
  （软失败不再截审计件、改名 `sent_soft` 前缀）已删除该 tag，verify.py 未同步更新。
  回归网当前是红的，等于「改坏没人拦」。
- `docs/工作原理与架构.md:89/:96/:102` 与 `docs/配置参考.md:20/:60-62` 仍描述
  `verify_fail`（硬失败 tag，迁移期已删）与 `verify_soft_fail`（审计 tag，现为 sent_soft
  命名降级路径），与代码现状脱节。

---

## 二、勘察结论（证据表）

本次不涉及新 DOM 锚点（沿用已验证的 data-e2e / slate / isFromMe 锚点），证据全部来自
代码 + 本机真实运行产物：

| 编号 | 结论 | 证据 |
|---|---|---|
| E1 | `publishRedBtn` 变红检查**不可作判据**（真发出时也会不红） | 01:22:40 私聊警告后强校验通过（气泡含发送文本） |
| E2 | 打字后**无任何「文字已进入编辑器」验证**，仅有不阻断的 warning | douyin.py:652-653 |
| E3 | 「编辑器已清空」铁证在空编辑器上恒真（空真漏洞） | douyin.py:667-669 + 01:23:23 群聊全程无正向证据却 success |
| E4 | 气泡软校验失败一律降级放行（`sent_soft` 命名，不截审计件） | douyin.py:687-700 + 截图 `002_012323_sent_soft_…png` |
| E5 | `verify.py` 断言已删除的 `verify_soft_fail` tag → 自检 RED，exit 1 | verify.py:175-177 + 实测 56/1 |
| E6 | 气泡回读文本比对在群聊下会命中本人旧消息（「<会话7>」），只宜作软信号 | 01:23:23 WARNING 文本 |
| E7 | 文档三处描述与代码脱节（verify_fail / verify_soft_fail 语义） | 架构.md:89,96,102；配置参考.md:20,60-62 |

---

## 三、目标与非目标

### 目标

1. **发送前获得正向证据「文字真实进入编辑器」**；失败自动重试一次，再失败以新审计
   tag `type_fail` 阻断并计 `failed_count`（进入 run() 正常失败分支，继续下一目标）。
   空/纯空白发送内容在 `_send_text` 入口直接拦截（配置错误，不入发送流程）——
   从另一侧堵死同一空真漏洞（评审 P1-1：`""==""` 恒真）。
2. 「编辑器清空」铁证保留，但**语义降为第二步**——只在证据 1 成立后才有判定效力。
3. 气泡回读软校验的放行/命名机制（`sent_soft`）保持不变，但它的每次放行现在都有
   「文字进过编辑器 + 发送后清空」双正向证据打底，误报/漏报窗口显著收窄。
4. `verify.py` 转 GREEN（exit 0），并把新保证写成防回归断言（先 RED 后 GREEN）。
5. 同步 `docs/工作原理与架构.md` 与 `docs/配置参考.md` 中所有相关表述。

### 非目标（本次不做，另行排期）

- 面板 XSS 转义（panel.html 执行记录表 :651-653 / 详情弹窗 :670-673 / 截图列表 :707
  三处 innerHTML 未复用已有的 `escapeHtml/escapeAttr`）。
- API CSRF/Origin 校验、截图路径 `startswith` 前缀陷阱（panel.py）。
- runner.py 15 分钟超时杀 daemon worker、空 targets 记 success、跨进程串行锁、
  系统定时任务时间漂移核对（01:56 vs 21:30）。
- 不改风控检测、拟人操作、会话匹配、扫描、面板 UI、定时任务、pyenv.py、隐私架构。

---

## 四、方案设计

### 4.1 编辑器落地正向证据（`douyin.py::_send_text`，评审 P1-1/P1-2 修订）

落地断言抽成方法 `_editor_text_equals(text)`，作为**无条件前置**；比较规则未来只改该方法体
（收窄应急出口，verify 断言不锁内联字面）。入口先拦截空/纯空白内容——否则等值比较在
空文本上恒真（`""==""`），漏洞从「打字没落地」扩大为「消息本身为空」也成立：

```python
# —— 步骤 0（新增，评审 P1-1）：入口拦截空/纯空白 ——
# 空/纯空白是配置错误：text.strip()=="" 与空编辑器等值恒真，直接 raise，不碰页面。
text = text.strip()
if not text:
    raise RuntimeError("发送内容为空或纯空白（配置问题），已跳过该目标以免空真误报成功。")

# —— 步骤 1：点击 + 输入（现状）——
self._human_click(el, "聊天输入框")
self._human_type(text)
time.sleep(random.uniform(0.3, 0.7))

# —— 步骤 2（新增）：正向证据 —— 文字必须真实进入编辑器 ——
# 否则「输入框清空=铁证」在空编辑器上恒真（空真），漏发也会被记成功。
# 聚焦/弹层属瞬态问题，先自动重试一次（用户决策），再失败才阻断。
if not self._editor_text_equals(text):
    logger.warning("文字未进入输入框（编辑器=%r），自动重试一次…", self._editor_text())
    time.sleep(random.uniform(0.5, 1.0))
    el2 = self._locate_chat_input()          # 重新定位，避免 handle 失效
    if el2:
        self._human_click(el2, "聊天输入框")
        self._human_type(text)
        time.sleep(random.uniform(0.3, 0.7))
if not self._editor_text_equals(text):
    self._audit_dump("type_fail", target_name)
    raise RuntimeError(
        "文字未能进入输入框（可能被弹层遮挡或焦点丢失），已跳过以免误判成功。")

# —— 步骤 3：发送（现状，微调）——
# publishRedBtn 检查降级为提示：E1 证明真发出也可能不红，不再暗示「可能没进编辑器」
self.page.keyboard.press("Enter")
...（Enter 未清空则补点发送按钮，逻辑不变）...
# 铁证：文字已确认进过编辑器（步骤 2）+ 发送后已清空
```

要点：

- **比较口径**：统一走 `_editor_text_equals(text)`——内部 `return self._editor_text() == text.strip()`；
  入口已 strip 并拦截空/纯空白（主口径「非空且等值」由此真正落地，不再只是口号）。
  若真实冒烟发现 slate 对特殊字符（emoji/换行/连续空格）有额外归一，**只改该方法体**
  收窄（如折叠空白），不放松「非空」主口径、不改判定流程（见风险表 R1）。
- **重试不重复发送**：重试只重做「点击 + 打字」，尚未按 Enter，无新增消息副作用；
  重试不叠加文本，依赖 `_human_type` 开头的 Ctrl+A+Backspace 预清空——该隐式依赖
  需在代码注释中标明（防未来改动 _human_type 时重试叠加洞复活）。
- **成功后流程不变**：Enter → 未清空补点按钮 → 清空铁证 → 气泡软校验 → sent/sent_soft
  截图命名 → 风控复检。

### 4.2 新审计 tag：`type_fail`

| tag | 语义 | 与现有 tag 边界 |
|---|---|---|
| `no_match` | 列表里没有这个名字 | 名字层面 |
| `switch_fail` | 点到了但右侧没切过去 | 切换层面 |
| `wrong_conversation` | 打开的不是目标会话（发送前复检） | 会话层面 |
| `no_editor` | 找不到聊天输入框 | 结构层面 |
| `type_fail`（新） | 编辑器在，但文字没进去（重试一次后仍失败） | **输入层面** |
| `send_fail` | 文字进去了、发送动作后编辑器仍有残留 | 发送层面 |

边界：`type_fail` 与 `send_fail` 互斥——`type_fail` 时还没按 Enter；只有通过步骤 2 才会
走到 `send_fail` 判定。两者都产出审计截图 + JSON、抛错、`failed_count+1`、继续下一目标。
空/纯空白入口拦截（4.1 步骤 0）属配置错误：raise 前无任何页面动作，不挂钩审计 tag
（不产审计件，run 级错误信息即足够定位），避免为配置错误发明 tag。

### 4.3 `verify.py` 修绿 + 防回归断言（先 RED 后 GREEN）

1. 审计 tag 循环（:175-177）：`"verify_soft_fail"` → `"type_fail"`。
   （当前 douyin.py 无 `type_fail` 字面量 → 改动后依然 1 FAIL，但失败原因变成
   「新保证未实现」，语义正确。）
2. 新增断言（全部先跑 RED，实现后转 GREEN，评审 P1-2：锁方法/形态，不锁内联字面）：
   - 文字没进输入框会重试并阻断：`'"type_fail"' in d` 且含「自动重试一次」marker；
   - 落地比较抽成方法：`"_editor_text_equals" in dfuncs`（AST）且
     `"self._editor_text_equals(text)" in d`（调用形态）；
   - 空/纯空白入口拦截：`"text = text.strip()" in d` 且含「纯空白」文案；
   - 误导性旧警告已删除：`"可能内容没进编辑器" not in d`；
   - 软校验降级路径保留（防误删）：`'"sent_soft"' in d`。
3. 同步更新 verify.py 内相关注释（原 :172-174 段仍引用 verify_soft_fail 语义）。

### 4.4 文档同步清单

| 文件:行 | 现状 | 改为 |
|---|---|---|
| `docs/工作原理与架构.md:89` | 发送动作描述：确认发送按钮变红 → 回车 | 加入「确认文字进入编辑器（经 `_editor_text_equals` 等值；失败自动重试一次，再失败审计 `type_fail`）→ 回车」；空/纯空白入口拦截；按钮红提示降级表述 |
| `docs/工作原理与架构.md:96` | `strict_verify=false` 记 `verify_soft_fail` 审计 | 描述真实语义：入口拦截空内容 + 编辑器落地 + 清空为无条件证据；气泡文本比对仅 strict=true 时附加；失配降级放行、截图前缀 `sent_soft`、不截审计件 |
| `docs/工作原理与架构.md:102` | tag 表含 `verify_fail` / `verify_soft_fail` | tag 表 = no_match / switch_fail / wrong_conversation / no_editor / type_fail / send_fail；注明软失败降级路径不调 `_audit_dump`、空内容拦截不挂钩 tag |
| `docs/配置参考.md:20` | `true`=清空+气泡双重校验；`false`=仅清空 | 无条件双证据：文字进入编辑器 + 发送后输入框清空（空/纯空白入口拦截）。`true`=再附加气泡文本比对；`false`=跳过气泡比对 |
| `docs/配置参考.md:60-62` | 同上的 YAML 注释 | 同上（含空拦截） |
| `config.yaml:14-16` | 同上的配置注释 | 同上（含空拦截） |
| `douyin.py:106-108` | `strict_verify` 成员注释（旧语义） | 随实现一并更新 |

---

## 五、错误处理

| 场景 | 行为 |
|---|---|
| 发送内容为空/纯空白（配置问题，评审 P1-1） | 入口 `text = text.strip()` 后 `if not text: raise RuntimeError` → `failed_count+1`（不碰页面、无审计件，见 4.2 边界注） |
| 打字后编辑器文本 ≠ 发送文本（第一次） | 自动重试一次（重新定位编辑器 → 点击 → 输入 → 再断言） |
| 重试后仍 ≠ | 审计 `type_fail`（截图+JSON）→ 抛错 → `failed_count+1` → run() 继续下一目标 |
| 重试时编辑器消失 | 视同 `type_fail`（仍审计原 tag，避免引入二义 tag） |
| Enter 后编辑器有残留 | `send_fail`（现状不变） |
| 气泡软校验失败 | 降级放行（现状不变，但此时有双正向证据打底），截图命名 `sent_soft_` |
| 风控 | 现状不变：`_check_risk_stop` / `needs_verify`，整轮停止 |

---

## 六、验证方案

项目无测试套件，沿用「`verify.py` 自检 + ad-hoc + 可选 live 冒烟」双轨（与仓库先例一致）。

1. **RED**：按 4.3 修改/新增 verify.py 断言 → 跑 `verify.py` → 确认 FAIL（且失败原因是
   「新保证未实现 / 旧警告还在」，不是笔误）。
2. **GREEN**：按 4.1/4.2 实现 douyin.py → 跑 `verify.py` → 确认全部通过、exit 0。
3. **文档**：按 4.4 同步后，verify.py 中若有文档相关断言一并过。
4. **live 冒烟（待授权，见八）**：对用户指定目标真实发送一次，观察日志出现
   「文字未进入输入框…重试」（仅在触发瞬态时）或直接强校验通过；核对截图命名。

**无法自动化的部分**（如实声明）：`type_fail` 触发依赖真实抖音 DOM 的瞬态条件
（弹层/焦点），离线无法复现；verify.py 只能锁代码结构，不能锁运行时行为。
该局限由 4.4 文档同步 + 冒烟观察弥补。

---

## 七、风险

| # | 风险 | 缓解 |
|---|---|---|
| R1 | 等值比较误判：slate 对 emoji/换行/连续空格等做额外归一，导致真输入也被判 type_fail | 比较统一走 `_editor_text_equals` 方法体（先 strip 后等值）；live 冒烟校准；若确有必要**只改该方法体**收窄（如折叠空白），不放松「非空」主口径、不破 verify 断言（断言不锁方法体） |
| R2 | 中文 IME/输入法态导致 keyboard.type 不落地 | 现状已在私聊目标成功输入过中文（E1 对照轮），风险低；且新断言正是此类问题的第一道显式防线 |
| R3 | 重试增加一次点击+打字，被风控放大 | 重试不产生任何发送动作（未按 Enter），无新增消息；介于目标间本就有 15-45s 随机间隔 |
| R4 | verify.py 断言过细绑实现形态（字符串 marker），未来重构误伤 | 遵循本仓库既有 verify.py 惯例（字符串/AST 断言），本次断言只锁「必须有该保证」的形态，不锁行号 |
| R5 | 文档与代码再次漂移 | 4.4 与实现同批提交；verify.py 注释同步更新 |

---

## 八、待确认

1. **live 冒烟授权**：验证第 4 步是否对真实目标发一条？发给谁（建议用户的小号或收藏夹会话，
   避免打扰真人；按仓库先例，**必须用户明确点头才执行**）。
2. strict_verify 文档表述按 4.4 是否接受？（行为不变，仅文档如实化。）

---

## 九、实施顺序（供 plan 参考）

1. `verify.py` 先 RED：tag 循环改 `type_fail` + 新增 4.3 断言 → 跑红确认
2. `douyin.py` 实现 4.1（入口拦截 + 落地断言方法化）/ 4.2（含注释同步）
3. `verify.py` 转 GREEN + 全绿（exit 0）
4. 4.4 文档同步（架构.md / 配置参考.md / config.yaml 注释）
5. （可选，待授权）live 冒烟；未授权则按 plan Task 3 收尾核对提示观察下次真实运行
