# MAI-001 多账号目录隔离（状态文件）

- **状态**: CODE_REVIEW + TEST（实施完成，评审与验证并行进行中）
- **特性**: 账号自定义别名 + 每账号独立环境/数据 + 面板账号切换
- **Spec**: `docs/superpowers/specs/2026-09-04-multi-account-isolation-design.md`（版本 2，已批准）
- **Plan**: `docs/superpowers/plans/2026-09-04-multi-account-isolation.md`（版本 2，用户签字 2026-09-06）
- **实施提交串**: `5d6dc02`(Task1 RED) → `0f52e09`(Task2 main 账号层+守卫) → `96dde10`(Task3 CLI+runner) → `de701d5`(Task4 panel) → `13eb699`(去重) → `162d12c`(Task5 panel.html) → `956012d`(Task6 文档，恰 7 文件)
- **Lead 独立核实（2026-09-06）**: verify **通过 101 / 失败 0、exit 0**；main.py `task_name`/`resolve_account` 各仅 1 处定义；无 `.running`/`panel_state.json` 残留；`userdata/accounts/` 为空（真实迁移属 Task 7 用户操作，符合预期）；git 仅 error.log 历史遗留
- **评审记录**: `docs/superpowers/reviews/MAI-001-spec-review.md`（APPROVED）；`MAI-001-plan-review.md`（APPROVED）；`MAI-001-code-review.md`（待出）
- **测试证据**: `docs/superpowers/test-results/MAI-001-IMPL.md`（待出）

## 事故记录（2026-09-06，Coder 自报，Lead 已核实处置）

Task 5 冒烟时 8765 端口挂着改造前的旧面板进程（双进程 bind 同端口），测试 POST /api/trigger 被旧进程按旧代码执行，12:58 向两个真实会话各发出一条 "x"（证据 `userdata/runs/20260906_125754/`，不可撤回）。处置：新旧面板进程均已停止（**用户的旧面板也被关闭，使用时需重启**）；被覆写的顶层 user_data.yaml 发送内容已恢复原值（Coder 核对）；无守卫/状态文件残留（Lead 核实）。教训已写入 Coder skill：冒烟前必查端口占用、未经授权只对动作端点做校验路径测试。此事故须记入 test-results 证据文件。

## 关卡推进记录（state 由 Lead 维护）

| 日期 | 阶段 | 说明 |
|---|---|---|
| 2026-09-05 | SPEC APPROVED | Reviewer 复核版本 2 APPROVED；spec 免签（71481d5） |
| 2026-09-05 | PLAN APPROVED | Reviewer 复核版本 2 APPROVED（P1×4/P2×6 关闭） |
| 2026-09-06 | IMPLEMENT | 用户签字（68bd66b）→ Coder 完成 Task 1-6 + 去重，verify 101/0 exit 0；期间发生真实发送事故（见上，已处置） |
| 2026-09-06 | **CODE_REVIEW + TEST** | Lead 派工 @reviewer 代码评审、@tester 逐 Task verify 留证（含事故记录）；两关过后 Lead 终审 + 交付 Task 7 人工核对清单 |
